# Evaluation outputs

## Current result

`s2g_pilot_rerun_20260913/` is the latest diagnostic run of the 20-query
`s2g_pilot_v1` benchmark. Its human columns remain blank; the accompanying
agent review is explicitly not an independent human evaluation.

## Archive

`archive/s2g_pilot_v1/` contains the superseded first pilot run. It is preserved
for provenance and must not be interpreted as the current result.

Runtime queries never contain ground truth. Ground truth is loaded only by the
offline evaluator after runtime artifacts are finalized.
